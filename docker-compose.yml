version: "3.8"

services:
  demo-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/app.db   # <<< VERY IMPORTANT
    volumes:
      - ./data:/app/data           # SQLite stored on host
    restart: unless-stopped
    